export enum StatusEnum {
  active = 'active',
  deleted = 'deleted',
}

export enum PropertyEnum {
  title = 'title',
  description = 'description',
}

export enum PropertyAltEnum {
  title = 'title',
}

export enum TypeEnum {
  video = 'video',
  ebook = 'ebook',
  exercise = 'exercise',
}

export enum QuestionTypeEnum {
  multiple = 'multiple',
}

export enum ParentalRatingEnum {
  Age0 = '0',
  Age10 = '10',
  Age12 = '12',
  Age14 = '14',
  Age16 = '16',
  Age18 = '18',
}
